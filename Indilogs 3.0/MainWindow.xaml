<Window x:Class="IndiLogs_3._0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:IndiLogs_3._0.ViewModels"
        xmlns:controls="clr-namespace:IndiLogs_3._0.Controls"
        xmlns:views="clr-namespace:IndiLogs_3._0.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="700" Width="1100" 
        Background="{DynamicResource BgDark}"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True" 
        Drop="Window_Drop"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.InputBindings>
        <KeyBinding Key="Space" Command="{Binding MarkRowCommand}" />
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoFilterOutCommand}" />
        <KeyBinding Key="OemPlus" Modifiers="Ctrl" Command="{Binding ZoomInCommand}" />
        <KeyBinding Key="Add" Modifiers="Ctrl" Command="{Binding ZoomInCommand}" />
        <KeyBinding Key="OemMinus" Modifiers="Ctrl" Command="{Binding ZoomOutCommand}" />
        <KeyBinding Key="Subtract" Modifiers="Ctrl" Command="{Binding ZoomOutCommand}" />
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding ToggleSearchCommand}" />
        <KeyBinding Key="Esc" Command="{Binding CloseSearchCommand}" />
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.15" ShadowDepth="2" BlurRadius="6"/>
            </Border.Effect>
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                        <Button Command="{Binding OpenSettingsCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                        Style="{DynamicResource ModernBtn}" 
                        Width="40" Height="40" ToolTip="Settings" Padding="0" Background="Transparent" BorderThickness="0">
                            <TextBlock Text="⚙" FontFamily="Segoe UI Symbol" FontSize="20" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondary}"/>
                        </Button>

                        <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="INDILOGS" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="VERSION 3.0" FontWeight="Normal" FontSize="10" Foreground="{DynamicResource PrimaryColor}" HorizontalAlignment="Right" Margin="0,-2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <Button Command="{Binding LoadCommand}" Style="{DynamicResource PrimaryBtn}" 
                    ToolTip="Load Files... (Drag &amp; Drop supported)" Width="140" Height="40">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📂" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="OPEN LOGS" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Background="{DynamicResource BgCard}" CornerRadius="20" Padding="5" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding MarkRowCommand}" Style="{DynamicResource ModernBtn}" 
                            Background="Transparent" ToolTip="Mark Row (Space)" Width="40" Height="30" Padding="0">
                                <TextBlock Text="📌" FontSize="16" Foreground="{DynamicResource TextPrimary}"/>
                            </Button>
                            <Button Content="⬇" Command="{Binding NextMarkedCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" ToolTip="Next Marked" Width="40" Height="30" Padding="0"/>
                            <Button Content="⬆" Command="{Binding PrevMarkedCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" ToolTip="Previous Marked" Width="40" Height="30" Padding="0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0" 
                        Visibility="{Binding IsLiveMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Border Background="{DynamicResource BgCard}" CornerRadius="6" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="5">
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding LivePlayCommand}" 
                                Visibility="{Binding IsPaused, Converter={StaticResource BooleanToVisibilityConverter}}"
                                Style="{DynamicResource ModernBtn}" ToolTip="Resume Live Monitoring" Background="Transparent" Foreground="{DynamicResource SuccessColor}" Padding="5,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="▶" Margin="0,0,5,0"/>
                                        <TextBlock Text="RESUME" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding LivePauseCommand}" 
                                Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                Style="{DynamicResource ModernBtn}" ToolTip="Pause Live Monitoring" Background="Transparent" Foreground="{DynamicResource WarningColor}" Padding="5,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⏸" Margin="0,0,5,0"/>
                                        <TextBlock Text="PAUSE" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>
                                <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="5,2"/>
                                <Button Command="{Binding LiveClearCommand}" Style="{DynamicResource ModernBtn}" ToolTip="Clear &amp; Skip Old Logs" Background="Transparent" Foreground="{DynamicResource DangerColor}" Padding="5,0">
                                    <TextBlock Text="⊘ CLEAR"/>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Button Command="{Binding OpenJiraCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Open JIRA" Width="40" Height="40" Padding="0">
                        <Image Source="/Resources/jira.ico" Width="22" Height="22" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                    <Button Command="{Binding OpenKibanaCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Open Kibana" Width="40" Height="40" Padding="0">
                        <Image Source="/Resources/kibana.ico" Width="22" Height="22" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                    <Button Command="{Binding OpenOutlookCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Send Email" Width="40" Height="40" Padding="0">
                        <Image Source="/Resources/outlook.ico" Width="22" Height="22" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" MinWidth="220" MaxWidth="400" Background="{DynamicResource BgPanel}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="7">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <TabControl Background="Transparent" BorderThickness="0" Margin="0" Padding="0" SelectedIndex="{Binding LeftTabIndex}">

                    <TabItem Header="EXPLORER">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="OPEN SESSIONS" Foreground="{DynamicResource TextSecondary}" FontWeight="Bold" FontSize="11" Margin="15,15,0,10"/>

                            <ListBox Grid.Row="1" 
                                     ItemsSource="{Binding LoadedSessions}" 
                                     SelectedItem="{Binding SelectedSession, Mode=TwoWay}"
                                     Background="Transparent" BorderThickness="0" 
                                     Foreground="{DynamicResource TextPrimary}"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     Margin="5,0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="📄" Margin="0,0,10,0" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding FileName}" ToolTip="{Binding FilePath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="10,6"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#15888888"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>

                            <Grid Grid.Row="2" VerticalAlignment="Bottom" Margin="10,0,10,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" 
                                        Background="{DynamicResource BgCard}" 
                                        BorderBrush="{DynamicResource BorderColor}" 
                                        BorderThickness="1" 
                                        CornerRadius="8" 
                                        Padding="10" 
                                        Margin="0,0,0,10"
                                        Visibility="{Binding IsExplorerMenuOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                                    </Border.Effect>

                                    <StackPanel>
                                        <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                            <TextBlock Text="Actions" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" FontSize="11"/>
                                        </DockPanel>

                                        <Button Command="{Binding ClearCommand}" Background="{DynamicResource DangerColor}" Style="{DynamicResource PrimaryBtn}" Margin="0,0,0,8">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="🗑 Clear All"/>
                                            </Viewbox>
                                        </Button>

                                        <Button Command="{Binding OpenMarkedLogsWindowCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="📌 Marked Lines"/>
                                            </Viewbox>
                                        </Button>
                                        <Button Command="{Binding OpenStatesWindowCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="🔄 States"/>
                                            </Viewbox>
                                        </Button>
                                        <Button Command="{Binding ExportParsedDataCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="📊 Export CSV"/>
                                            </Viewbox>
                                        </Button>
                                        <Button Command="{Binding RunAnalysisCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="⚙ Run Analysis"/>
                                            </Viewbox>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <Button Grid.Row="1" 
                                        Command="{Binding ToggleExplorerMenuCommand}"
                                        Background="{DynamicResource BgCard}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        HorizontalAlignment="Left"
                                        Width="40" Height="40"
                                        ToolTip="Show Actions">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                                    Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    CornerRadius="20">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="5" ShadowDepth="2"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource BgCardHover}"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>

                                    <Grid>
                                        <TextBlock Text="••" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-8,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExplorerMenuOpen}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="✕" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExplorerMenuOpen}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Button>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="LOGGERS">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TreeView Grid.Row="0" ItemsSource="{Binding LoggerTreeRoot}" 
                                      Background="Transparent" 
                                      BorderThickness="0" 
                                      Padding="10"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                                      Foreground="{DynamicResource TextPrimary}">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                                        <EventSetter Event="PreviewMouseRightButtonDown" Handler="TreeViewItem_PreviewMouseRightButtonDown"/>
                                        <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                        <Setter Property="ContextMenu">
                                            <Setter.Value>
                                                <ContextMenu>
                                                    <MenuItem Header="🎯 Show Only This" Command="{Binding PlacementTarget.Tag.TreeShowOnlyThisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}"/>
                                                    <MenuItem Header="📂 Show With Children" Command="{Binding PlacementTarget.Tag.TreeShowWithChildrenCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}"/>
                                                    <Separator/>
                                                    <MenuItem Header="🚫 Hide This Logger" Command="{Binding PlacementTarget.Tag.TreeHideThisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}"/>
                                                    <MenuItem Header="🚫 Hide With Children" Command="{Binding PlacementTarget.Tag.TreeHideWithChildrenCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}"/>
                                                    <Separator/>
                                                    <MenuItem Header="✅ Show All" Command="{Binding PlacementTarget.Tag.TreeShowAllCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                </ContextMenu>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal" Margin="2">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Count, StringFormat=' ({0})'}" Foreground="{DynamicResource TextSecondary}" Margin="5,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <HierarchicalDataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                <Setter Property="TextBlock.TextDecorations" Value="Strikethrough"/>
                                                <Setter Property="TextBlock.Foreground" Value="{DynamicResource DangerColor}"/>
                                            </DataTrigger>
                                        </HierarchicalDataTemplate.Triggers>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>

                            <Grid Grid.Row="1" VerticalAlignment="Bottom" Margin="10,0,10,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" 
                                        Background="{DynamicResource BgCard}" 
                                        BorderBrush="{DynamicResource BorderColor}" 
                                        BorderThickness="1" 
                                        CornerRadius="8" 
                                        Padding="10" 
                                        Margin="0,0,0,10"
                                        Visibility="{Binding IsExplorerMenuOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                                    </Border.Effect>

                                    <StackPanel>
                                        <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                            <TextBlock Text="Actions" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" FontSize="11"/>
                                        </DockPanel>

                                        <Button Command="{Binding OpenMarkedLogsWindowCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5">
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                                <TextBlock Text="📌 Marked Lines"/>
                                            </Viewbox>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <Button Grid.Row="1" 
                                        Command="{Binding ToggleExplorerMenuCommand}"
                                        Background="{DynamicResource BgCard}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        HorizontalAlignment="Left"
                                        Width="40" Height="40"
                                        ToolTip="Show Actions">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                                    Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    CornerRadius="20">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="5" ShadowDepth="2"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource BgCardHover}"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>

                                    <Grid>
                                        <TextBlock Text="••" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-8,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExplorerMenuOpen}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="✕" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExplorerMenuOpen}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Button>
                            </Grid>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True">
                <GridSplitter.Style>
                    <Style TargetType="GridSplitter">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="7">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </GridSplitter.Style>
            </GridSplitter>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,15,40,0" Panel.ZIndex="100"
                        Background="{DynamicResource BgCard}" 
                        CornerRadius="20" Padding="8,4"
                        Visibility="{Binding IsSearchPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.4" Color="Black"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0,5,0" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox x:Name="SearchTextBox" 
                                 Width="250" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="Transparent" Foreground="{DynamicResource TextPrimary}" 
                                 BorderBrush="Transparent" BorderThickness="0"
                                 Padding="2" VerticalContentAlignment="Center"
                                 CaretBrush="{DynamicResource PrimaryColor}"
                                 IsVisibleChanged="SearchTextBox_IsVisibleChanged"/>
                        <Button Content="✕" Command="{Binding CloseSearchCommand}" 
                                Style="{DynamicResource ModernBtn}" Width="24" Height="24" Padding="0" Margin="5,0,0,0"
                                Background="Transparent"
                                ToolTip="Close (Esc)"/>
                    </StackPanel>
                </Border>

                <TabControl x:Name="MainTabs" Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}" BorderThickness="0" Background="Transparent">

                    <TabItem Header="PLC LOGS">
                        <DataGrid x:Name="MainLogsGrid" ItemsSource="{Binding Logs}" SelectedItem="{Binding SelectedLog}" 
                                  FontSize="{Binding GridFontSize}" 
                                  FontFamily="{DynamicResource ContentFontFamily}"
                                  FontWeight="{DynamicResource ContentFontWeight}"
                                  AutoGenerateColumns="False" IsReadOnly="True" 
                                  RowStyle="{DynamicResource LogRowStyle}" 
                                  SelectionMode="Extended"
                                  SelectionUnit="FullRow"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PreviewKeyDown="MainLogsGrid_PreviewKeyDown"
                                  RequestBringIntoView="DataGrid_RequestBringIntoView"
                                  Loaded="DataGrid_Loaded">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <EventSetter Event="RequestBringIntoView" Handler="DataGrid_Cell_RequestBringIntoView"/>
                                    <EventSetter Event="MouseLeftButtonDown" Handler="DataGrid_Cell_MouseDown"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Command="{Binding PlacementTarget.DataContext.FilterContextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Header="Time Focus Filter"/>
                                    <Separator/>
                                    <MenuItem Header="Filter Out (Message)..." Command="{Binding FilterOutCommand}"/>
                                    <MenuItem Header="Filter Out (Thread)..." Command="{Binding FilterOutThreadCommand}"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.InputBindings>
                                <KeyBinding Key="Space" Command="{Binding MarkRowCommand}" />
                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ViewLogDetailsCommand}" CommandParameter="{Binding SelectedLog}" />
                            </DataGrid.InputBindings>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Lvl" Binding="{Binding Level}" Width="50" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Date, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="170" FontFamily="{StaticResource ContentFontFamily}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Thread" Binding="{Binding ThreadName}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ThreadName}" Value="Manager">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Message" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <controls:HighlightTextBlock 
                                                Text="{Binding Message}" 
                                                HighlightText="{Binding DataContext.SearchText, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                VerticalAlignment="Center">
                                                <controls:HighlightTextBlock.Style>
                                                    <Style TargetType="controls:HighlightTextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </controls:HighlightTextBlock.Style>
                                            </controls:HighlightTextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="PLC (FILTERED)">
                        <DataGrid x:Name="FilteredLogsGrid" ItemsSource="{Binding FilteredLogs}" SelectedItem="{Binding SelectedLog}" 
                                  FontSize="{Binding GridFontSize}" 
                                  FontFamily="{DynamicResource ContentFontFamily}"
                                  FontWeight="{DynamicResource ContentFontWeight}"
                                  AutoGenerateColumns="False" IsReadOnly="True" 
                                  RowStyle="{DynamicResource LogRowStyle}"
                                  SelectionMode="Extended"
                                  SelectionUnit="FullRow"
                                  HorizontalScrollBarVisibility="Disabled"
                                  RequestBringIntoView="DataGrid_RequestBringIntoView">
                            <DataGrid.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ViewLogDetailsCommand}" CommandParameter="{Binding SelectedLog}" />
                            </DataGrid.InputBindings>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Lvl" Binding="{Binding Level}" Width="50" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Date, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="170" FontFamily="{StaticResource ContentFontFamily}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Thread" Binding="{Binding ThreadName}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ThreadName}" Value="Manager">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Message" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <controls:HighlightTextBlock 
                                                Text="{Binding Message}" 
                                                HighlightText="{Binding DataContext.SearchText, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                VerticalAlignment="Center">
                                                <controls:HighlightTextBlock.Style>
                                                    <Style TargetType="controls:HighlightTextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </controls:HighlightTextBlock.Style>
                                            </controls:HighlightTextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="APP LOGS">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="{DynamicResource BgPanel}" Padding="8" CornerRadius="4" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Button Command="{Binding FilterAppErrorsCommand}" 
                                            Style="{DynamicResource ModernBtn}" 
                                            Background="{DynamicResource DangerColor}" Foreground="White" 
                                            Padding="10,6" Margin="5" ToolTip="Show Errors Only (Newest First)">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚠" Margin="0,0,8,0"/>
                                            <TextBlock Text="SHOW ERRORS ONLY" FontWeight="Bold"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>

                            <DataGrid Grid.Row="1" x:Name="AppLogsGrid" 
                                      ItemsSource="{Binding AppDevLogsFiltered}" 
                                      SelectedItem="{Binding SelectedLog}"
                                      Sorting="AppLogsGrid_Sorting"
                                      FontSize="{Binding GridFontSize}" 
                                      FontFamily="{DynamicResource ContentFontFamily}"
                                      FontWeight="{DynamicResource ContentFontWeight}"
                                      AutoGenerateColumns="False" IsReadOnly="True" 
                                      RowStyle="{DynamicResource LogRowStyle}" 
                                      SelectionMode="Extended" SelectionUnit="FullRow"
                                      HorizontalScrollBarVisibility="Disabled"
                                      RequestBringIntoView="DataGrid_RequestBringIntoView">
                                <DataGrid.InputBindings>
                                    <KeyBinding Key="Space" Command="{Binding MarkRowCommand}" />
                                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ViewLogDetailsCommand}" CommandParameter="{Binding SelectedLog}" />
                                </DataGrid.InputBindings>
                                <DataGrid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding PlacementTarget.DataContext.FilterContextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Header="Time Focus Filter"/>
                                        <Separator/>
                                        <MenuItem Header="Filter Out (Message)..." Command="{Binding FilterOutCommand}"/>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Lvl" Binding="{Binding Level}" Width="60" FontWeight="Bold" CanUserSort="True" SortMemberPath="Level">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Timestamp" Binding="{Binding Date, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="170" CanUserSort="True" SortMemberPath="Time">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Binding="{Binding Logger}" Header="Logger" Width="250" CanUserSort="True" SortMemberPath="Logger">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Thread" Binding="{Binding ThreadName}" Width="100" CanUserSort="True" SortMemberPath="Thread">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ThreadName}" Value="Manager">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="Message" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <controls:HighlightTextBlock 
                                                    Text="{Binding Message}" 
                                                    HighlightText="{Binding DataContext.SearchText, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                    VerticalAlignment="Center">
                                                    <controls:HighlightTextBlock.Style>
                                                        <Style TargetType="controls:HighlightTextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Level}" Value="ERROR">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                                    <Setter Property="Foreground" Value="Black"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </controls:HighlightTextBlock.Style>
                                                </controls:HighlightTextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="EVENTS">
                        <DataGrid ItemsSource="{Binding Events}" 
                                  FontSize="{Binding GridFontSize}" 
                                  FontFamily="{DynamicResource ContentFontFamily}" 
                                  FontWeight="{DynamicResource ContentFontWeight}"
                                  AutoGenerateColumns="False" IsReadOnly="True" 
                                  RowStyle="{DynamicResource LogRowStyle}" 
                                  CanUserAddRows="False" 
                                  VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="180"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" FontWeight="Bold"/>
                                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="120" Foreground="{DynamicResource PrimaryColor}"/>
                                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80"/>
                                <DataGridTextColumn Header="Parameters" Binding="{Binding Parameters}" Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Parameters}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Info" Binding="{Binding Description}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="SCREENSHOTS">
                        <Grid Background="{DynamicResource BgDark}" ClipToBounds="True">
                            <ScrollViewer x:Name="ScreenshotScrollViewer"
                                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                          PanningMode="Both" Cursor="SizeAll"
                                          PreviewMouseLeftButtonDown="OnImageMouseLeftButtonDown"
                                          PreviewMouseLeftButtonUp="OnImageMouseLeftButtonUp"
                                          PreviewMouseMove="OnImageMouseMove"
                                          PreviewMouseWheel="OnScreenshotMouseWheel">
                                <ItemsControl ItemsSource="{Binding Screenshots}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#333" BorderThickness="1" Margin="20">
                                                <Image Source="{Binding}" 
                                                   Width="{Binding DataContext.ScreenshotZoom, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                   Stretch="Uniform"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>

                    <TabItem Header="CONFIG">
                        <TextBox Text="{Binding PressConfig}" IsReadOnly="True" 
                                 FontFamily="{DynamicResource ContentFontFamily}"
                                 Background="{DynamicResource BgDark}" Foreground="{DynamicResource TextPrimary}" 
                                 BorderThickness="0" VerticalScrollBarVisibility="Auto" Padding="10"/>
                    </TabItem>

                    <TabItem Header="SETUP INFO">
                        <TextBox Text="{Binding SetupInfo}" IsReadOnly="True" 
                                 FontFamily="{DynamicResource ContentFontFamily}"
                                 Background="{DynamicResource BgDark}" Foreground="{DynamicResource TextPrimary}" 
                                 BorderThickness="0" VerticalScrollBarVisibility="Auto" Padding="10"/>
                    </TabItem>

                    <TabItem Header="GRAPHS">
                        <views:GraphsView DataContext="{Binding GraphsVM}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </TabItem>
                </TabControl>

                <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#444" ShowsPreview="True">
                    <GridSplitter.Style>
                        <Style TargetType="GridSplitter">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="2">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </GridSplitter.Style>
                </GridSplitter>

                <Border Grid.Row="2" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="2">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{DynamicResource BgDark}">
                            <Border Background="{DynamicResource BgPanel}" BorderThickness="0,2,0,0" BorderBrush="{DynamicResource PrimaryColor}" Padding="15,6">
                                <TextBlock Text="SELECTED LOG DETAILS" FontWeight="Bold" FontSize="11" Foreground="{DynamicResource PrimaryColor}"/>
                            </Border>
                        </StackPanel>

                        <Grid Grid.Row="1" Margin="10,8,10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Time:" Foreground="{DynamicResource TextSecondary}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding SelectedLog.Date, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="3" Text="Level:" Foreground="{DynamicResource TextSecondary}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="4" Text="{Binding SelectedLog.Level}" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="6" Text="Logger:" Foreground="{DynamicResource TextSecondary}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="7" Text="{Binding SelectedLog.Logger}" Foreground="{DynamicResource PrimaryColor}" TextTrimming="CharacterEllipsis"/>
                        </Grid>

                        <TextBox Grid.Row="2"
                                 Text="{Binding SelectedLog.Message, Mode=OneWay}"
                                 IsReadOnly="True"
                                 TextWrapping="NoWrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 AcceptsReturn="True"
                                 FontFamily="{StaticResource ContentFontFamily}"
                                 FontSize="13"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="10"
                                 Margin="0,5,0,0"
                                 Foreground="{DynamicResource TextPrimary}"/>
                    </Grid>
                </Border>

                <Border Grid.Row="3" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="15,8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="7">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="🔍 Edit Filters..." Command="{Binding OpenFilterWindowCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgCard}"/>
                            <Button Content="🎨 Color Rules..." Command="{Binding OpenColoringWindowCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgCard}"/>
                            <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="10,4"/>
                            <CheckBox Content="APPLY FILTER OUT" IsChecked="{Binding IsFilterOutActive}" 
                                      ToolTip="Uncheck to reveal hidden logs" VerticalAlignment="Center" Margin="10,0,15,0" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                            <CheckBox Content="APPLY FILTER" IsChecked="{Binding IsFilterActive}" 
                                      VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True">
                <GridSplitter.Style>
                    <Style TargetType="GridSplitter">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="7">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </GridSplitter.Style>
            </GridSplitter>

            <Border Grid.Column="4" Width="200" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,0,0,0" Padding="15">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=MainTabs}" Value="7">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <DockPanel>
                    <TextBlock Text="SAVED CONFIGURATIONS" Foreground="{DynamicResource TextSecondary}" FontWeight="Bold" FontSize="11" Margin="0,0,0,15" DockPanel.Dock="Top"/>

                    <Grid DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" 
                                Background="{DynamicResource BgCard}" 
                                BorderBrush="{DynamicResource BorderColor}" 
                                BorderThickness="1" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,0,10"
                                Visibility="{Binding IsConfigMenuOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>

                            <StackPanel>
                                <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                    <TextBlock Text="Actions" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" FontSize="11"/>
                                </DockPanel>

                                <Button Command="{Binding SaveConfigCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource SuccessColor}" Foreground="White" Margin="0,0,0,5">
                                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                        <TextBlock Text="💾 Save Current"/>
                                    </Viewbox>
                                </Button>
                                <Button Command="{Binding LoadConfigCommand}" Style="{DynamicResource ModernBtn}" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5">
                                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                        <TextBlock Text="📂 Load Selected"/>
                                    </Viewbox>
                                </Button>
                                <Button Command="{Binding RemoveConfigCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" BorderBrush="{DynamicResource DangerColor}" Foreground="{DynamicResource DangerColor}" BorderThickness="1">
                                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                        <TextBlock Text="🗑 Delete Selected"/>
                                    </Viewbox>
                                </Button>
                            </StackPanel>
                        </Border>

                        <Button Grid.Row="1" 
                                Command="{Binding ToggleConfigMenuCommand}"
                                Background="{DynamicResource BgCard}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                HorizontalAlignment="Left"
                                Width="40" Height="40"
                                ToolTip="Show Actions">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" 
                                            Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            CornerRadius="20">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="5" ShadowDepth="2"/>
                                        </Border.Effect>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgCardHover}"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>

                            <Grid>
                                <TextBlock Text="••" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-8,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsConfigMenuOpen}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="✕" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsConfigMenuOpen}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Button>
                    </Grid>

                    <ListView ItemsSource="{Binding SavedConfigs}" 
                              SelectedItem="{Binding SelectedConfig, Mode=TwoWay}"
                              Background="Transparent" 
                              BorderThickness="0" 
                              Foreground="{DynamicResource TextPrimary}"
                              HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4" Background="Transparent">
                                    <Grid.InputBindings>
                                        <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DataContext.ApplyConfigCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                    </Grid.InputBindings>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Name="Border" Padding="8" CornerRadius="4" Background="Transparent">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryGlow}"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                                    <Setter TargetName="Border" Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#15888888"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource PrimaryColor}" Padding="10,0">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Logs.Count, StringFormat='Logs: {0:N0}'}" Foreground="White" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding Events.Count, StringFormat='Events: {0:N0}'}" Foreground="White" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.9"/>
            </Grid>
        </Border>

        <Grid Grid.RowSpan="3" Background="#CC000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Panel.ZIndex="999">
            <Border Background="{DynamicResource BgPanel}" 
                    BorderBrush="{DynamicResource PrimaryColor}" 
                    BorderThickness="0" 
                    CornerRadius="30" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Width="450" 
                    Height="300">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="80" ShadowDepth="0" Opacity="0.9" Color="{Binding Color, Source={StaticResource PrimaryColor}}"/>
                </Border.Effect>

                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Width="140" Height="140" VerticalAlignment="Top" HorizontalAlignment="Center">
                        <Ellipse StrokeThickness="6" Width="120" Height="120" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="{Binding Color, Source={StaticResource AnimColor1}}" BlurRadius="25" ShadowDepth="0" Opacity="1"/>
                            </Ellipse.Effect>
                            <Ellipse.Stroke>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{Binding Color, Source={StaticResource AnimColor1}}" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="0.8"/>
                                </LinearGradientBrush>
                            </Ellipse.Stroke>
                            <Ellipse.RenderTransform>
                                <RotateTransform Angle="0" CenterX="60" CenterY="60"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                             From="0" To="360" Duration="0:0:1.5" RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>

                        <Ellipse StrokeThickness="4" Width="90" Height="90" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="{Binding Color, Source={StaticResource AnimColor2}}" BlurRadius="20" ShadowDepth="0" Opacity="1"/>
                            </Ellipse.Effect>
                            <Ellipse.Stroke>
                                <LinearGradientBrush StartPoint="1,0" EndPoint="0,1">
                                    <GradientStop Color="{Binding Color, Source={StaticResource AnimColor2}}" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="0.8"/>
                                </LinearGradientBrush>
                            </Ellipse.Stroke>
                            <Ellipse.RenderTransform>
                                <RotateTransform Angle="360" CenterX="45" CenterY="45"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                             From="360" To="0" Duration="0:0:2" RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>

                        <Ellipse Width="60" Height="60" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Ellipse.Fill>
                                <RadialGradientBrush>
                                    <GradientStop Color="{Binding Color, Source={StaticResource PrimaryColor}}" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </RadialGradientBrush>
                            </Ellipse.Fill>
                            <Ellipse.Effect>
                                <DropShadowEffect Color="{Binding Color, Source={StaticResource PrimaryColor}}" BlurRadius="30" ShadowDepth="0" Opacity="0.8"/>
                            </Ellipse.Effect>
                            <Ellipse.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" CenterX="30" CenterY="30"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             From="0.7" To="1.2" Duration="0:0:1.2" RepeatBehavior="Forever" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             From="0.7" To="1.2" Duration="0:0:1.2" RepeatBehavior="Forever" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0.3" To="0.8" Duration="0:0:1.2" RepeatBehavior="Forever" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>

                        <TextBlock Text="{Binding CurrentProgress, StringFormat='{}{0:0}%'}" 
                                   FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AnimText}" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="{Binding Color, Source={StaticResource AnimText}}" BlurRadius="15" ShadowDepth="0" Opacity="0.7"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Grid>

                    <TextBlock Grid.Row="1" 
                               Text="{Binding StatusMessage}" 
                               FontSize="16" 
                               Foreground="{DynamicResource TextPrimary}" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Top" 
                               TextAlignment="Center" 
                               TextWrapping="Wrap" 
                               Margin="0,10,0,0"
                               MaxHeight="80"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>