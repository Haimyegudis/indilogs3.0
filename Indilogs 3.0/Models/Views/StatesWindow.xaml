<Window x:Class="IndiLogs_3._0.Views.StatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System States Timeline" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgDark}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Detected States (Thread: Manager)" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextPrimary}"/>

        <DataGrid Grid.Row="1" x:Name="StatesGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Background="{DynamicResource BgCard}"
                  RowStyle="{DynamicResource LogRowStyle}"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Transition (From -> To)" Binding="{Binding TransitionTitle}" Width="250" FontWeight="SemiBold"/>

                <DataGridTemplateColumn Header="Result" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" 
                                       Foreground="{Binding StatusColor}" 
                                       FontWeight="Bold" 
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="160"/>
                <DataGridTextColumn Header="End Time" Binding="{Binding EndTime, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="160"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="100"/>
            </DataGrid.Columns>

            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" 
                  Command="{Binding FilterToStateCommand}" 
                  CommandParameter="{Binding ElementName=StatesGrid, Path=SelectedItem}"/>
            </DataGrid.InputBindings>
        </DataGrid>

        <Button Grid.Row="2" Content="Close" Click="Close_Click" Style="{DynamicResource ModernBtn}" HorizontalAlignment="Right" Margin="0,10,0,0" Width="80"/>
    </Grid>
</Window>