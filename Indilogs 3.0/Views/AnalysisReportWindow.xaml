<Window x:Class="IndiLogs_3._0.Views.AnalysisReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Analysis Report" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgDark}"
        Foreground="{DynamicResource TextPrimary}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="RunItemContainerStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="0"
                                CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BgCardHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="PROCESS HISTORY" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,0,0,15" Opacity="0.7"/>

                <ListBox x:Name="RunsList" Grid.Row="1"
                         ItemsSource="{Binding AllResults}" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemContainerStyle="{StaticResource RunItemContainerStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.RowSpan="2" Grid.Column="0" Width="4" Background="{Binding StatusColor}" CornerRadius="2" Margin="0,0,10,0"/>

                                <TextBlock x:Name="NameTxt" Grid.Column="1" Grid.Row="0" 
                                           Text="{Binding ProcessName}" 
                                           FontWeight="SemiBold" FontSize="14" 
                                           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>

                                <TextBlock x:Name="StatusTxt" Grid.Column="1" Grid.Row="1" 
                                           Text="{Binding Status}" 
                                           Foreground="{Binding StatusColor}" 
                                           FontSize="12" FontWeight="Bold"/>

                            </Grid>

                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                    <Setter TargetName="NameTxt" Property="Foreground" Value="White"/>

                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="{DynamicResource BgDark}">
            <TextBlock Text="Select a process from the list to view details."
                       VerticalAlignment="Center" HorizontalAlignment="Center" 
                       Foreground="{DynamicResource TextSecondary}"
                       FontSize="16" Opacity="0.5">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedItem, ElementName=RunsList}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Grid DataContext="{Binding SelectedItem, ElementName=RunsList}" Margin="40">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ProcessName}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>

                        <Border Background="{DynamicResource BgCard}" BorderBrush="{Binding StatusColor}" BorderThickness="1" CornerRadius="4" Padding="10,2" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontWeight="Bold" FontSize="14"/>
                        </Border>
                    </StackPanel>

                    <TextBlock Text="Execution Summary" Foreground="{DynamicResource TextSecondary}" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <Separator Grid.Row="1" Background="{DynamicResource BorderColor}" Margin="0,20"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="RESULT OVERVIEW" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding Summary}" Foreground="{DynamicResource TextPrimary}" FontSize="15" TextWrapping="Wrap" LineHeight="22" Margin="0,0,0,25"/>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Failure">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Warning">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <TextBlock Text="ROOT CAUSE &amp; ERRORS" Foreground="#FF5555" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>

                            <ItemsControl ItemsSource="{Binding ErrorsFound}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#1A252525" BorderBrush="#33FF0000" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8" Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="❌" Margin="0,2,10,0" VerticalAlignment="Top"/>
                                                <TextBlock Grid.Column="1" Text="{Binding}" 
                                                           Foreground="#FF6B6B" 
                                                           FontWeight="SemiBold" 
                                                           FontSize="13" 
                                                           TextWrapping="Wrap" 
                                                           FontFamily="Consolas"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <Border Background="{DynamicResource BgCard}" CornerRadius="6" Padding="20" Margin="0,10,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Success">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="🎉 Process Completed Successfully" Foreground="{DynamicResource SuccessColor}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                                <TextBlock Text="No anomalies detected in the log flow." Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="3" Content="Close Report" Click="Close_Click" 
                        Style="{DynamicResource ModernBtn}" 
                        HorizontalAlignment="Right" Width="120" Height="35" Margin="0,20,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</Window>